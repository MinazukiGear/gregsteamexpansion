# 大型蒸汽采矿厂设计

本文档是「大型蒸汽采矿厂」(旗舰系统 F1)的设计文档。**全部 `12` 个设计议题已于 2026-09-08 由用户拍板收口,同日数值复核定案**(节拍/耗汽/概率表配置化),无待定项;控制器配方成本仍标"提案值"。

## 文档状态与讨论规则

- 当前状态:`12` 个议题全部确认——身份注册、定位与配置开关、产出池与概率、结构外观、动力与蒸汽、产量与三档难度、库存与输出、运行控制与禁用行为、表现与信息、获取配方、与既有裁决的关系、上游基准。纯虚空语义(议题 3)经用户明确定案。
- 实际游戏测试与最终验收由用户执行;进入代码实现前需用户明确授权。
- 基准环境:Minecraft `1.20.1`、Forge `47.4.10`、GTCEu Modern `7.5.3`。

## 文档索引

- [已确认决策](#已确认决策)(议题 1–12)
- [上游基准(已核对源码)](#上游基准已核对源码)
- [立项前提](#立项前提)
- [验收清单(由用户执行)](#验收清单由用户执行)

## 已确认决策

### 身份与注册(议题 1)

| 项目 | 已确认值 |
| --- | --- |
| 中文名 | 大型蒸汽采矿厂 |
| 英文名 | Large Steam Ore Plant |
| 资源 ID | `gregsteamexpansion:large_steam_ore_plant` |
| Java 注册字段 | `LARGE_STEAM_ORE_PLANT` |
| 控制器机器类 | `LargeSteamOrePlantMachine` |
| 科技阶段 | 蒸汽时代收官旗舰,与锅炉房(钢级及以上)构成闭环 |
| 配方类型 | **无**(非配方类型机器,自定义生产逻辑,同锅炉房先例) |

### 定位与配置开关(议题 2)

- **虚空产矿终端**:不与世界任何方块、矿脉、区块数据交互,产出来源为纯生成(用户 2026-09-08 拍板议题 3 = a:与地点/世界生成完全无关)。
- **配置开关**:`large_steam_ore_plant_enabled`(默认 `true`,重启生效,与难度配置同口径)。禁用时:注册保持完整(存档兼容)、结构与物品仍可获取,但**机器不可运行**,GUI/状态显示"已在配置中禁用"(用户议题 7 定案)。
- **不是**世界交互采石场:不破坏任何方块、不下探矿杆(用户 2026-09-08 改写原旗舰语义)。

### 产出池与概率(议题 3)

- **产出条目 = 注册名 + 权重 + 数量**;金属矿物产出 `rawOre` **原矿**(喂给 S1 洗矿厂 → A4 研磨厂 → C0 离心机现有链,不绕过任何处理环节),非金属产出其标准掉落物(与上游矿机 fortune 0 掉落口径一致)。
- **权重表可配置**(用户 2026-09-08 复核定案):配置文件提供条目列表,格式 `<物品注册名>|<权重>|[数量]`(数量省略为 1),如 `gtceu:raw_iron|15`、`minecraft:redstone|6|4`;**空表/非法条目回退内置默认表**并记录警告日志;配置重启生效(与启用开关同口径)。
- **内置默认概率表(权重和 114)**:

| 矿物 | 产出 | 权重 |
| --- | --- | ---: |
| 煤 | 煤炭 ×1 | 20 |
| 铁 | 原矿(铁) ×1 | 15 |
| 铜 | 原矿(铜) ×1 | 15 |
| 锡 | 原矿(锡) ×1 | 12 |
| 铅 | 原矿(铅) ×1 | 10 |
| 锌 | 原矿(锌) ×1 | 10 |
| 镍 | 原矿(镍) ×1 | 8 |
| 金 | 原矿(金) ×1 | 6 |
| 红石 | 红石 ×4 | 6 |
| 铝土 | 原矿(铝) ×1 | 4 |
| 青金石 | 青金石 ×4 | 4 |
| 钻石 | 钻石 ×1 | 2 |
| 绿宝石 | 绿宝石 ×1 | 2 |

- 单次抽取按**当前生效表**(配置加载后)按权重独立随机选出一种矿物;GUI 悬浮提示展示生效表。
- 概率表不与难度联动;难度只放大份数(议题 6)。

### 结构与外观(议题 4,按建议口径)

- 包围盒 `9×9×7`,四个水平朝向;两区外壳(家族口径):
  - 底面、顶面与 4 条竖棱:工业蒸汽机械方块(**182** 格 = 162 面 + 20 棱,含控制器);
  - 四面墙体(第 2–6 层,每层 28 格):蒸汽机械方块,共 **140** 格,仓室唯一可替换区。
- **选矿工位阵列**:第 2 层与第 6 层内部各 9 个蒸汽研磨方块(`x,z ∈ {3,5,7}`,3×3 间隔网格,共 **18** 格,破碎-选矿语义);
- 其余内部 **227** 格固定空气;外壳合计 `182 + 140 = 322` 格。
- 控制器位于正面底层水平正中。

#### 逐层结构图(草案)

符号:`I` 工业、`B` 蒸汽墙体(可替换)、`M` 蒸汽研磨方块、`A` 空气、`C` 控制器。行序正面 → 背面。

第 1 层(底):`IIIIICIII` 于正面行,其余 `IIIIIIIII` ×8 行。
第 2、6 层(工位):`IBBBBBBBI` / `BAAAAAAAB` / `BAMAMAMAB` / `BAAAAAAAB` / `BAMAMAMAB` / `BAAAAAAAB` / `BAMAMAMAB` / `BAAAAAAAB` / `IBBBBBBBI`。
第 3–5 层(空心):`IBBBBBBBI` / `BAAAAAAAB` ×7 / `IBBBBBBBI`。
第 7 层(顶):`IIIIIIIII` ×9 行。

### 动力与蒸汽消耗(议题 5)

- 仅接受 GTCEu 标准蒸汽,不消耗 EU;**无配方类型**,蒸汽按固定生产节拍结算。
- **4 个生产工位**,单工位节拍 **200 tick** 完成 1 次抽取(用户 2026-09-08 复核定案);单工位耗汽 **3,000 mB/t**。
- 满速(4 工位并行)= **12,000 mB/t**,需 `ceil(12,000 ÷ 1,200) = 10` 个供给仓——对齐锅炉房产能档(议题 5 用户确认"是");钢级锅炉房(13,500 mB/t)恰好支撑一台满速采矿厂,钨钢级(48,000 mB/t)可同时驱动四台。
- 仓室上限复核:10 供给仓 + 1 排气仓 + ≥1 物品输出总线 = 12 ≤ **16**(上限不变)。
- 供汽结算沿用家族口径:逐刻跨全部供给仓原子取汽,单仓 1,200 mB/t 机器侧上限,位置稳定序;断汽回退 1 tick 续跑。
- 等效成本(定案口径):`3,000 × 200 = 600,000 mB(=300,000 EU 等效)/份 @Expert`——虚空产出的旗舰溢价;Easy 档按产出份数放大后折合约 75,000 EU 等效/份。

### 产量与三档难度(议题 6)

- **产出倍率(用户定案,新口径)**:`Easy ×4 / Normal ×2 / Expert ×1`。
- 解释口径:每工位每次抽取的**产出份数**放大(Expert 1 份 → Normal 2 份 → Easy 4 份);耗汽、节拍、概率表三档相同。
- 与既有口径的关系:B1/B2 为 `Easy 2× / Normal / Expert 1×`,本机为首次三档全差异;按 `difficulty.md` 全局设置接入规则在实现时登记受控字段。
- 倍率对每次抽取即时生效(非配方机器,无批次锁定;正在进行的节拍按启动时档位结算)。
- 期望产量(定案):满速 4 工位,每 200 tick 4 次独立抽取 → Expert **4 份/200t(期望 1 份/50t)**、Normal **8 份/200t**、Easy **16 份/200t**。

### 库存与输出(议题 7)

- 控制器无内部物品缓存;产物一次性写入物品输出总线(≥1,跨总线/槽拆分堆叠)。
- 输出阻塞则暂停生产节拍(家族口径),空间恢复后续跑;概率表在阻塞期间不消耗蒸汽。
- 流体输出:**无**(本机不产出流体;不设任何流体接口——蒸汽流体输入/输出仓均不入结构)。

### 运行控制与禁用行为(议题 8)

- 标准 GTCEu 启停控制:软锤 + GUI 电源按钮;暂停期间不取汽、不推进。
- 结构失效:进度回退 1 tick,修复后续跑。
- 配置禁用(议题 2/7):优先级最高——即使结构完整、供汽充足、输出畅通,机器不启动,状态栏显示"已在配置中禁用"。
- 排气仓:必须且只能 1 个(大型机规则),受阻冻结进度(不回退),反馈脉冲与热伤害沿用通用规则。

### 表现与信息(议题 9)

- GUI 沿用家族运行信息页骨架(状态/进度/蒸汽合计/每刻需求/输出摘要),新增:
  - **概率表悬浮提示**(当前生效权重表——配置覆盖后以配置为准,中英双语);
  - 工位状态行:当前节拍剩余 tick、上周期产出摘要(矿物 ×份数 ×4 工位);
  - 配置禁用时的红色状态行。
- 声效沿用研磨机(MACERATOR)声;配方推进时在工位位置叠加破碎烟尘粒子。
- 控制器正面两态纹理走 `steamMultiblockModel` 的 `addTextureOverride` 模式(纹理原创绘制,家族配色)。
- 两级物品提示:结构、概率表、三档倍率、耗汽、禁用行为;中英双语。

### 获取配方(议题 10,提案值)

- 九宫格家族骨架(核心居中 + 上下传动 + 左右构件 + 四角板),恒产 1,三档相同,不含仓室:

```text
P G P
C M C
P G P
```

| 符号 | 输入 | 数量 |
| --- | --- | ---: |
| `M` | 蒸汽研磨方块(破碎选矿核心) | 1 |
| `G` | 青铜齿轮 | 2 |
| `C` | 青铜构件 | 2 |
| `P` | 青铜板 | 4 |

- 配方 ID `gregsteamexpansion:shaped/large_steam_ore_plant`;旗舰定位,成本低于 B1(研磨方块核心与其自身配方折算后约 4 齿轮、3 构件、8 板),复核时可加重。

### 与既有裁决的关系(议题 11)

- **B5 抽水站撤销**(原版水源覆盖)不适用:主世界矿石没有原版级替代来源,本机不构成冗余。
- **A5 煤气门控**不触及:矿石不是"煤气在热解炉之前"式的进度门控对象;洗矿-研磨-离心链保持完整。
- **多方块储罐 / 冷凝水回收**:不涉及。
- **蒸汽涡轮 / EU**:不涉及;蒸汽是唯一动力。

### 上游基准(已核对源码)

- `BedrockOreMinerMachine`(电力多方块):读取区块矿脉存档数据(`BedrockOreVeinSavedData`),按机器档位产出原矿/破碎矿/矿石块,超频 +50%,矿脉会耗竭(`depleteVein`,depletedYield 保底)。
- `MinerLogic` / `SteamMinerLogic`(单方块蒸汽矿机):只挖 `Tags.Blocks.ORES` 世界方块,镐具战利品表(fortune 0),矿杆下探。
- **本机差异(用户定案)**:不读任何世界数据(纯虚空)、不耗竭、不设矿杆、不破坏方块;产出形态对齐上游 fortune 0 口径(金属=原矿、非金属=标准掉落);概率权重为模组自有定案表。

### 立项前提

1. **定位:旗舰收官**(锅炉房 → 采矿厂闭环:燃料 → 蒸汽 → 矿物),第一台非配方类型的产出机器(锅炉房先例:自定义生产逻辑)。
2. **配置开关是平衡阀**:虚空产出天然敏感,启用权交给整合包/服务器。
3. **产出形态保护既有链**:只产原矿/标准掉落,不跳过洗矿-研磨-离心环节。

### 立项必填核对

| 必填项 | 对应议题 |
| --- | --- |
| 中文名、英文名、资源 ID、注册字段、机器类、科技阶段 | 议题 1(✅) |
| 产出机制(替代配方槽位) | 议题 3(✅ 概率表定案 + 配置权重表) |
| 结构尺寸、固定核心、可替换接口及数量上下限 | 议题 4(✅ 逐层图与仓室上限) |
| 蒸汽换算率与每刻需求 | 议题 5(✅ 12,000 mB/t 满速,10 供给仓) |
| 并行公式、处理时间 | 议题 5(✅ 固定 4 工位 × 200 tick,无并行机制) |
| 三档受控字段具体值 | 议题 6(✅ Easy 4× / Normal 2× / Expert 1×,仅产份数) |
| 配置开关 | 议题 2(✅ 默认启用,禁用=不可运行) |
| 与已实现机器的复用关系 | 议题 5/7/8(✅ 家族供汽/输出/排气口径) |
| 验收清单 | 见下(待用户执行) |

### 验收清单(由用户执行)

**结构与成型**

- [ ] `9×9×7` 按逐层图成型;外壳 322(工业 182 + 墙体 140)、研磨工位 18、内部空气 227。
- [ ] 仓室仅替换墙体位置,合计 ≤16(供给仓 ≥1 + 排气仓必须且只能 1);排气受阻冻结进度。
- [ ] 结构可共用;四个水平朝向均可成型。

**生产与概率**

- [ ] 满速 12,000 mB/t(10 供给仓足供);断汽回退 1 tick 续跑。
- [ ] 长时间运行产出分布与当前生效权重表一致(大样本统计);金属=原矿、非金属=标准掉落;配置修改权重并重启后分布随之变化。
- [ ] Expert 满速期望 1 份/50t(4 份/200t);Normal 8 份/200t;Easy 16 份/200t;概率表与耗汽三档不变。

**库存与控制**

- [ ] 产出写入物品输出总线,阻塞暂停且不耗汽;软锤/电源按钮启停正确。
- [ ] 配置禁用时不可运行并显示禁用状态;恢复配置并重启后可运行,结构/库存无损。

**表现与获取**

- [ ] GUI 概率表悬浮提示与产出摘要正确;中英文本地化完整;两态纹理正常。
- [ ] 控制器按定案配方合成,恒产 1。

## 文档定案状态

- `12` 个议题已全部确认(2026-09-08):纯虚空语义、配置开关、概率表、结构、耗汽、三档倍率(Easy 4×/Normal 2×/Expert 1×,首例三档全差异)、禁用行为均由用户拍板。
- 同日复核定案:单工位节拍 200 tick、耗汽 3,000 mB/t(满速 12,000 mB/t、10 供给仓);**概率权重表开放配置**(条目格式 `<注册名>|<权重>|[数量]`,空/非法回退内置默认表,重启生效)。
- 提案值待复核(仅剩):控制器配方成本。
- 本文档设计决策全部收口;进入代码实现前需用户明确授权。

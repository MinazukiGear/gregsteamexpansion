# 大型蒸汽锻压厂旧大纲（已归档）

候选编号 B6 已定名为“蒸汽锻压机”，正式设计、结构、配方和验收清单统一维护于 [steam-forge.md](steam-forge.md)。本路径仅保留旧链接入口，不再维护第二份设计或待办。

当前已实现身份为 `gregsteamexpansion:steam_forge`，注册字段为 `GSEMachines.STEAM_FORGE`，实现类为 `SteamForgeMachine`；结构为 `3×3×5`，最大并行 `8`。

旧大纲中的“大型蒸汽锻压厂”、`large_steam_forge` 与并行 `16` 均为定案前建议，不是新增机器或后续实现任务。原始讨论保留在 Git 历史中；实际游戏验收状态以正式规格为准，仍待用户执行。

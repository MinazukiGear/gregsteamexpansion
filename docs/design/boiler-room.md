# 锅炉房（多方块蒸汽锅炉）设计文档

> 状态：**决策已定（P0–P3 全部锁定），四档代码已实现，启动、持续供气、四档满产、协同燃烧边界、结构接口、进气与缺粉冷却规则已验收，其余项目待游戏内验收**。本文档是「锅炉房」的唯一设计文件，同时存放设计决策、具体参数与待办项（项目约定：每台机器只建一个 md）。
>
> 目标：以 GTCEu 大型锅炉（`LargeBoilerMachine`）为骨架，融入本模组单方块混合燃料锅炉（`MixedFuelBoilerMachine`）的液体燃料 + 协同燃烧语义，形成蒸汽时代的终端多方块锅炉，并在四个 tier 上产能完全上位于 GTCEu 大型锅炉。
>
> **已确认的方向（用户拍板）**：
> - **A**「完全取代」= **产能上的完全上位**（不是燃料形式对等）。锅炉房每个 tier 的产出必须严格高于同档 GTCEu 大型锅炉，不要求支持固体燃料。
> - **B**「取代任何大型锅炉」= GTCEu 大型锅炉有**四档**（青铜 / 钢 / 钛 / 钨钢），锅炉房必须提供与之对应的四档变体，且每档都产能上位。需要给出更高级（钛、钨钢）锅炉的数据。
> - **C** 协同燃烧粉料机制 = 整体复用单方块逻辑（见 P1#8）。
>
> 标注约定：✅ = 已锁定；💡 = 建议默认值（用户尚未拍板，实现时可先用，但需回用户确认）；🚫 = 硬性约束。

---

## 一、依据（已核对源码）

- **GTCEu `LargeBoilerMachine`**（v7.5.3）
  - 结构：3×3×4（火室 X 至少 3，管道 P 1，控制器 S 1，外壳 C 至少 20）。
  - 配方类型：`LARGE_BOILER_RECIPES`（纯固体燃料物品 + 水 → 蒸汽）。
  - 产汽公式：`maxDrain = 温度 × 节流 × 5 / (steamPerWater × 100)`，`steamGenerated = 抽水量 × steamPerWater`，每 5 tick 一周期，温度 ≥ 100 才产汽。
  - **公式化简**：`steamGenerated = 温度 × 节流 / 20`（mB/tick）—— `steamPerWater` 在分子分母同时出现会抵消，故基准产出只由 `温度 × 节流` 决定，与 `steamPerWater` 取值无关。
  - 四档注册（`GTMultiMachines`，`registerLargeBoiler`）：青铜 / 钢 / 钛 / 钨钢，各自独立 `maxTemperature` 与 `heatSpeed`（见下「四档基准」）。
  - 节流：25%–100%，调节燃料燃烧时长。
  - 干烧：产汽周期内水抽不够即爆炸（强度 2）。
  - 排气：消音器（Muffler）部件；难度倍率经 `LargeBoilerMachineMixin` 包裹 `steamPerWater` 字段读取注入（注意：因上式抵消，mixin 对继承类实际是否改变产出需复核，本模组锅炉房已改为显式规则，见 P2#12）。
- **本模组 `MixedFuelBoilerMachine`**（`mixed-fuel-boiler.md`）
  - 配方类型：`STEAM_BOILER_RECIPES`（流体燃料 + 水 → 蒸汽，协同燃烧加粉料物品）。
  - 满温固定速率：液体 16/40 mB/t（低/高压），协同 24/60 mB/t；再乘难度倍率（Easy/Normal `5×`、Expert `2×`）。
  - 双模式持久不自动降级；协同燃烧 +50% 产量、约 +15% 综合效率；缺粉暂停并加速冷却；高温断水后再进水爆炸（强度 2）。
  - 温度：最高 500/1000，起始产汽 100，升温每 24/12 tick +1，停机冷却每 45/40 tick −1，协同缺粉冷却每 20 tick −1。
- **现有孤儿资产**（锅炉房只消化蒸汽进气室，见「孤儿资产消化主线」）：工业蒸汽机械方块、蒸汽进气室、蒸汽流体输入仓、蒸汽流体输出仓。

---

## 二、GTCEu 大型锅炉四档基准与产能目标

> 数据来自 `ConfigHolder$MachineConfigs$LargeBoilers` 默认值与 `GTMultiMachines` 注册调用（v7.5.3 反编译核实）。

| 档位 | `maxTemperature` | `heatSpeed`（每 N tick +1°C） | 外壳方块 | GTCEu 基准产出* | 锅炉房目标（≥） |
| --- | ---: | ---: | --- | ---: | ---: |
| 青铜 bronze | 800 | 1 | `CASING_BRONZE_SOLID` | 4,000 mB/t | **6,000 mB/t** |
| 钢 steel | 1,800 | 1 | `CASING_STEEL_SOLID` | 9,000 mB/t | **13,500 mB/t** |
| 钛 titanium | 3,200 | 1 | `CASING_TITANIUM_STABLE` | 16,000 mB/t | **24,000 mB/t** |
| 钨钢 tungstensteel | 6,400 | 2 | `CASING_TUNGSTENSTEEL_ROBUST` | 32,000 mB/t | **48,000 mB/t** |

\* 基准产出 = `maxTemperature × 100 / 20`（mB/tick，steamPerWater=160、节流100%）。本模组难度规则（P2#12：仅 Easy ×2，Normal/Expert ×1）不改变四档相对量级（约 1 : 2.25 : 4 : 8）；锅炉房对比基准：Normal/Expert = 同档 GTCEu ×1.5，Easy = ×3。

**锅炉房对应的四档目标产出（仅协同燃烧 +50%，P1#4=A + P1#5）**：青铜 ≥ 6,000 / 钢 ≥ 13,500 / 钛 ≥ 24,000 / 钨钢 ≥ 48,000 mB/t，逐档严格高于同档 GTCEu 大型锅炉 → 满足「产能完全上位」（用户拍板 A/B）。

---

## 三、P0 — 架构级 ✅ 已决定

### 1. 类继承关系 → ✅ 选项 A：继承 GTCEu `LargeBoilerMachine`

- 复用：节流 ± 按钮与燃烧时长缩放、运行期干烧爆炸、消音器排气接口、`steamPerWater` 字段（保留 mixin 契约，但产出倍率改用 P1#4 显式公式）。
- 需重写：`recipeLogic` 消费改为流体燃料 + 协同粉料（见 P0#3）；重写产汽逻辑时**不能绕过** mixin 命中的 `steamPerWater` 字段读取点。

### 2. 定位与去重声明 → ✅ 完全取代 GTCEu 大型锅炉（产能上位）+ 覆盖其全部四档变体

- 「完全取代」的口径（用户拍板 A）：**产能上的完全上位**，不要求燃料形式对等。锅炉房每个 tier 产出必须严格高于同档 GTCEu 大型锅炉（见「四档基准」目标列）。
- 「任何大型锅炉」（用户拍板 B）：GTCEu 大型锅炉有**四档**——青铜 / 钢 / 钛 / 钨钢。锅炉房须提供与之对应的**四档变体**，逐档产能上位，而非仅青铜/钢两档。
- 由此 P0#3 的新配方类型**只需覆盖「流体燃料 + 协同粉料 + 水」**（协同燃烧语义），无需纳入纯固体燃料路径——早期纯固体燃料仍由本模组单方块固体锅炉兜底。

### 3. 配方类型 → ✅ 选项 A：新开一个多方块配方类型

- 新类型 `BOILER_ROOM_RECIPES`（P0#3=A），只接受：
  - 流体燃料（液体，经 GTCEu 标准流体输入仓或多重流体输入仓）
  - 协同粉料物品（经物品输入仓，必须带 `co_firing_dust_fuels` 标签）
  - 水（经流体输入仓，用多重仓时与流体燃料可共用一仓）
- **输出**：蒸汽（经输出仓，产量见 P1#4）。
- **不支持纯固体燃料**（P1#5 仅协同燃烧；纯固体由单方块固体锅炉兜底，与 P0#2-A「产能上位非燃料对等」一致）。
- 数据包可扩展：流体燃料白名单、协同粉料标签均标签驱动。
- 由继承类的 `recipeLogic` 消费；上游 `LARGE_BOILER_RECIPES` 纯固体路径**不再用于本机器**。

---

## 四、P1 — 核心机制 ✅ 已决定（4–8 全部锁定）

### 4. 蒸汽产量模型 → ✅ 选项 A：GTCEu 温度 × 节流缩放，协同 +50% 作产出乘子

继承 `LargeBoilerMachine` 的温度×节流公式，叠加协同 +50% 与显式难度倍率：

```
baseOutput = currentTemperature × throttle / 20          // 继承类原生公式，steamPerWater 抵消
coFiringOutput = baseOutput × 1.5                         // 协同燃烧 +50%（P1#4）
finalOutput = coFiringOutput × difficultyMultiplier       // 显式难度倍率（P2#12）
```

- 满温（throttle=100%）下各档满产 = `maxTemperature × 100 / 20 × 1.5 × 难度`，即「四档基准」目标表数值。
- 协同燃烧 +50% 作为产出乘子叠加（乘到 `steamPerWater` 读取结果或等效位置，不绕过 mixin 注入点）。

### 5. 工作模式 → ✅ 仅保留协同燃烧模式

- 锅炉房只有「协同燃烧」一种运行模式（液体燃料 + 协同粉料），**四个 tier 均如此**，不保留纯液体模式、不保留纯固体模式。
- ~~⚠️ 与 P0#2「完全取代 GTCEu 大型锅炉」的潜在冲突~~ —— **已消解（用户拍板 A/B）**：上位靠产能（+50% 协同，见 P1#4）而非燃料形式对等。早期纯固体燃料由单方块固体锅炉兜底。
- GUI 移除模式切换按钮（见 P3#13）。

### 6. 温度与安全数值 → ✅ 已决定（四档定值）

- **`maxTemperature` 必须用 GTCEu 同档值（800/1800/3200/6400）**——这是「产能上位」的前提：同档 maxTemperature 下，锅炉房靠协同 +50%（P1#4）即可严格高于上游；若调低 maxTemperature 则产出低于同档 GTCEu，违背 P0#2-A。要更强/更弱改协同乘子或节流上限，不要动 maxTemperature 下限。
- ⚠️ **硬约束（用户）**：协同缺粉冷却须**逐档快于**停机冷却**与**升温（即每降 1°C 所需 tick 少于两者）。

| 项 | 青铜 | 钢 | 钛 | 钨钢 |
| --- | ---: | ---: | ---: | ---: |
| `maxTemperature` | 800 | 1,800 | 3,200 | 6,400 |
| 起始产汽温 | 100 | 100 | 100 | 100 |
| 升温（每 N tick +1°C） | 20 | 12 | 8 | 5 |
| 停机冷却（每 N tick −1°C） | 30 | 20 | 15 | 10 |
| 协同缺粉冷却（每 N tick −1°C） | 15 | 10 | 6 | 4 |

- 备选（未采用）：直接照搬单方块数值（24/12、45/40、20）保持蒸汽时代完全一致（但只有两级，与四档要求冲突）；或沿用 GTCEu 大型锅炉原生 heatSpeed（1/1/1/2，升温更快）。

### 7. 干烧与爆炸规则 → ✅ 选项 A：GTCEu 运行期「水抽不够即爆炸（强度 2）」

- 继承 `LargeBoilerMachine` 即得，与 P0#1 一致。不采用单方块「断水后再进水爆炸」以免双爆。

### 8. 协同燃烧粉料机制 → ✅ 选项 A：整体复用单方块逻辑

- 复用单方块 `MixedFuelBoilerMachine` 逻辑：
  - 粉料须带 `co_firing_dust_fuels` 标签。
  - 粉料提供约 30% 基础液体热值（具体折算沿用单方块实现）。
  - **缺粉**：暂停燃烧进度，并触发加速冷却（每 N tick −1°C 取 P1#6 协同缺粉冷却值）。
- 与 P1#5 已锁定的「仅协同燃烧」模式一致；四档变体共用同一套粉料机制，仅产出按各自 maxTemperature 缩放。

---

> **用户更新定案（2026-09-12）**：取消“顶面中心固定且仅 1 个进气室”，改为顶面中央沿长轴的 **11 格**仅允许对应档外壳或进气室，整条 **至少 1 个、最多 11 个**，正中心不再必装。其他仓室不得放入该条带；第二层火室两侧保持空气。

## 五、P2 — 接口与结构 ✅ 已锁定（2026-09-12 更新进气室条带规则）

### 9. 结构图案 → ✅ 尺寸、选型、位置与数量规则已定

- 四档变体实现：**四个独立注册机器**（bronze/steel/titanium/tungstensteel），统一实例化继承 `LargeBoilerMachine` 的 `BoilerRoomMachine`，以构造参数区分档位，maxTemperature 取 800/1800/3200/6400；共用 `GSEBoilerPatterns` 图案定义。
- 结构尺寸（用户拍板）：**长 7 × 宽 11 × 高 7 去角长方体**（四档共用同一 footprint）。
- **去角定义**：去掉四条横向棱，具体效果为**底层（底面）与顶层（顶面）由 7×11 变为 5×11**（沿 7 轴每端各收 1 格）；中间 5 层仍为 7×11 周边环。
- 外壳数量核算：
  - 7×11×7 完整表面壳 = 314 格（底面 7×11=77 + 顶面 77 + 中间 5 层各 32 周边 = 160）。
  - 去角后：底面 5×11=55 + 顶面 55 + 中间 5 层各 32 = **270 格**外壳表面。
  - 扣控制器 1、消音器 1、底层火室 27 后，剩 **241 格**：**230 个通用外壳候选位 + 11 个进气条带位**。安装 N 个进气室（1≤N≤11）时，普通外壳为 `241−N−标准仓室数量`，不设最低数；**第二层火室 27 格位于内部，不占外壳位**。
- > ⚠️ **尺寸不影响产汽**：产出由 P1#4 的「温度 × 节流」公式按 tier 决定，与体积无关。7×11×7 是终端级大建造造型，不是产出缩放——同档产出与「四档基准」目标表一致。若希望体积参与产出，需改产汽模型（不在当前设计内）。

**🚫 硬性约束（用户）**：锅炉房**不可使用任何蒸汽输入输出仓室**——包括蒸汽流体输入仓、蒸汽流体输出仓、蒸汽排气仓；**物品输入同样禁用蒸汽物品输入总线**，必须用 GTCEu 标准物品输入仓。涉及流体进出的统一改用 GTCEu 标准流体输入仓 / 输出仓；泄压走消音器（见 P2#11）。**蒸汽进气室除外**（它是助燃空气前置，非流体进出的蒸汽仓，见 P2#10）。

**✅ 流体输入口径（用户已定）**：流体输入允许使用 GTCEu **多重流体输入仓**，单仓即可同时容纳水与液体燃料（此前"水/液燃是否共用"的待决项就此解决）；标准单流体仓也可用，此时需 ≥2 个。

**部件位置（用户拍板）**：
- **控制器**：正面（外接 7×7 面）中心。
- **消音器**：背面（外接 7×7 面）中心。
- **蒸汽进气室**：顶面（5×11）中央沿长轴的 11 格条带，每格仅允许对应档外壳或进气室，进气室总数 1–11 个，正中心非必装。条带以外不接受进气室。
- **管道**：填充控制器中轴——正面中心到背面中心的连线（X=4、Z=4、Y=2…10，共 9 格），两端分别为控制器与消音器；管道位于结构内部，不占外壳位。
- **燃烧室（火室）**：底层与第二层**中心的 3×9**——底层（Z=1，5×11 面）中央 3×9（X=3…5、Y=2…10，替换 casing 27 格）；第二层（Z=2，7×11 环）中央 3×9 同坐标（填充内部空气位 27 格）。共 **54 格**。与中轴管道（Z=4）、控制器/消音器（Z=4）、进气室（Z=7）无重叠。
- **输入/输出仓室**：流体输入仓、物品输入仓、输出仓**各至少 1 个**；位置在 230 个通用外壳候选位自选，不可放入顶面进气条带。

| 槽位 | 方块（已定） | 数量 | 说明 |
| --- | --- | --- | --- |
| 外壳 `C` | 按档 GTCEu 锅炉 casing（见 P3#16） | 230 个通用候选位 | 可替换为允许的标准输入/输出接口；不引入工业蒸汽机械方块 |
| 进气条带 `A` | 按档外壳或蒸汽进气室 | 11 格 | 顶面中央长条，仅这两类候选；其中进气室至少 1 个 |
| 燃烧室 `X` | 沿 GTCEu 大型锅炉火室 | **54**（已锁定） | 底层与第二层中心的 3×9 各一片（27+27）；底层片替换 casing、第二层片填充内部 |
| 管道 `P` | 沿 GTCEu 大型锅炉管道 | **9**（已由中轴锁定） | 控制器中轴 Y=2…10，连通正面控制器与背面消音器 |
| 控制器 `S` | 锅炉房控制器 | **1**（已定） | 正面（外接 7×7）中心 |
| 流体输入仓 | GTCEu 标准流体输入仓或多重流体输入仓 | **≥1** | 多重仓单仓可同时容纳水与液燃；标准单流体仓则需 ≥2；禁用蒸汽流体输入仓 |
| 物品输入仓 | GTCEu 标准物品输入仓 | **≥1** | 协同粉料；**禁用蒸汽物品输入总线**（用户已定） |
| 输出仓 | GTCEu 标准输出仓 | **≥1** | 蒸汽导出；禁用蒸汽流体输出仓 / 排气仓 |
| 消音器 | GTCEu Muffler | **1**（已定） | 背面（外接 7×7）中心；泄压，禁用蒸汽排气仓 |
| 蒸汽进气室 | 蒸汽进气室 | **1–11**（已定） | 顶面中央 11 格条带内任意位置；协同燃烧硬性前置，见 P2#10 |

**✅ 数量上限口径（2026-09-11 用户定案，2026-09-12 更新条带）**：不设普通外壳最低数量，也不设旧 `casing ≥220` 派生的仓室合计 `≤20` 限制。230 个通用候选位可替换为允许的标准接口；另外 11 格仅允许外壳/进气室。上述数目是位置核算，不是必须保留的外壳数量。

### 10. 蒸汽进气室的机械语义 → ✅ 选项 A：协同燃烧硬性前置

- **必须接蒸汽进气室（提供助燃空气）才能进入协同燃烧模式**；无进气室则锅炉房不产汽 / 不运行。给该孤儿最明确的归宿，且语义自洽（协同燃烧需要鼓风）。
- ✅ **空气消耗量随档位递增**（用户已定方向）：四个锅炉房的空气消耗量逐档递增。**定值（用户确认）**：**青铜 50 / 钢 100 / 钛 200 / 钨钢 400 mB/t**（约 ×2 逐档递增）。
- 进气室数量已锁：**1–11 个**（2026-09-12 用户更新）；未接时 GUI/状态栏提示条带位置与最低数量。控制器汇总所有进气室的空气，整刻需求模拟成功后才扣除；合计不足时不部分扣气。

> **供气核算与验收边界**：每仓仍每 80 tick 收集 4,000 mB（平均 50 mB/t），不接受外部注入。按平均收支，青铜/钢/钛/钨钢分别需 1/2/4/8 个有效采集仓覆盖标称耗气，11 格条带可以容纳；低于该数量仍允许成型，但缓存可能耗尽。2026-09-12 已完成持续供气验收：采集与缓存收支正常，缓存耗尽会暂停燃烧，恢复供气后继续运行并持续产汽。

### 11. 排气 / 泄压 → ✅ 保持使用 GTCEu 消音器（不使用蒸汽排气仓）

- 蒸汽导出走**输出仓**（见 P2#9）；超压 / 满槽泄压沿用 GTCEu 大型锅炉的**消音器（Muffler）部件**行为，不引入本模组蒸汽排气仓。
- 蒸汽排气仓**不用于锅炉房**（它不是导出蒸汽的仓室，此为已澄清的用法边界）。

### 12. 难度倍率接入细节 → ✅ 仅 Easy 下蒸汽产量 2×，Normal / Expert 不变

- 显式难度规则：**Easy ×2，Normal ×1，Expert ×1**。直接用于协同产出计算，不依赖 `LargeBoilerMachineMixin` 隐式行为（公式化简后 mixin 对继承类产出可能无效，显式更稳）。
- 对比基准：Normal/Expert 下锅炉房产出 = 同档 GTCEu × 1.5（协同 +50%）；Easy 下 ×3，仍严格上位（满足 P0#2-A/B）。
- 重写产汽逻辑时不绕过 mixin 命中的 `steamPerWater` 读取点（保持与继承类契约一致）。

---

## 六、P3 — 呈现与收尾 ✅ 已确认（13–16 全部锁定）

> 依据本模组既有注册/命名/纹理约定（`GSEMachines.java`：`.multiblock(id, class::new)` + `.pattern` + `.appearanceBlock(GTBlocks.CASING_…)` + `.model(steamMultiblockModel(overlayDir))` + `.langValue(英文)` + `.tooltipBuilder(...)`；多方块本体世界内搭建，控制器为可合成物品）。

### 13. GUI → ✅ 保留 GTCEu 大型锅炉显示与节流，叠加协同粉料进度

- 保留节流 ± 按钮、温度计、产汽/状态显示（与 P0#1 一致）。实现入口为 `BoilerRoomMachine#createUI`：沿用上游多方块滚动显示和节流点击处理，复用锅炉纹理增加温度与粉料仪表。此前写作 `SteamBoilerUI` 子类的路径不适用于上游大型锅炉，修正实现描述，不改变呈现要求。
- 因 P1#5「仅协同燃烧」为单一模式，**移除模式切换按钮**。
- 新增：协同粉料消耗进度条（借鉴 `MixedFuelBoilerMachine` 的粉料进度显示），与温度/产汽状态同屏。
- 新增：未成型界面显示“顶面中央 11 格条带内安装 1–11 个进气室”的结构要求（见 P2#10）；成型后显示缺空气、缺粉料与协同粉料缓冲状态。仪表进度由服务端同步，悬浮文本使用同步后的百分比。

### 14. 合成配方 → ✅ 已定（材料定稿）

- 四个档位的锅炉房控制器均为**可合成物品**；多方块本体仍世界内用方块搭建（GTCEu 惯例）。
- 配方形态：**对应档 GTCEu 大型锅炉控制器 + 本模组混合燃料锅炉（低/高压）** → 对应档锅炉房控制器（使「完全取代」在获取路径上成立）。
- ✅ **四档共用同一配方模板，仅换 tier 材质**（用户已定）。
- ✅ **同时拥有工作台配方和组装机配方**（用户已定），两条路线产出相同、材料一致。
- ✅ **材料定稿（用户）**，四档模板：

  | 档位 | 输入 | 输出 |
  | --- | --- | --- |
  | 青铜 | `gtceu:bronze_large_boiler` ×1 + 本模组**高压**混合燃料锅炉 ×1 + **青铜板** ×7 | `gse:boiler_room_bronze` ×1 |
  | 钢 | `gtceu:steel_large_boiler` ×1 + 高压混合燃料锅炉 ×1 + **钢金板**（steel 板）×7 | `gse:boiler_room_steel` ×1 |
  | 钛 | `gtceu:titanium_large_boiler`（对应 tier 大型锅炉）×1 + 高压混合燃料锅炉 ×1 + **钛板** ×7 | `gse:boiler_room_titanium` ×1 |
  | 钨钢 | `gtceu:tungstensteel_large_boiler` ×1 + 高压混合燃料锅炉 ×1 + **钨钢板** ×7 | `gse:boiler_room_tungstensteel` ×1 |

### 15. 命名 / 注册 → ✅ 四档独立 `MultiblockMachineDefinition`，共用一个类

| 档位 | 资源 ID | 注册字段 | 中文名 | 英文名 |
| --- | --- | --- | --- | --- |
| 青铜 | `gse:boiler_room_bronze` | `BOILER_ROOM_BRONZE` | 锅炉房（青铜） | Boiler Room (Bronze) |
| 钢 | `gse:boiler_room_steel` | `BOILER_ROOM_STEEL` | 锅炉房（钢） | Boiler Room (Steel) |
| 钛 | `gse:boiler_room_titanium` | `BOILER_ROOM_TITANIUM` | 锅炉房（钛） | Boiler Room (Titanium) |
| 钨钢 | `gse:boiler_room_tungstensteel` | `BOILER_ROOM_TUNGSTENSTEEL` | 锅炉房（钨钢） | Boiler Room (Tungstensteel) |

- 四档共用类 **`BoilerRoomMachine`**，`maxTemperature` 经构造参数区分（同 `LargeBoilerMachine` 做法）。
- 注册方式：`.multiblock(id, BoilerRoomMachine::new)` + `.pattern(...)` + `.appearanceBlock(GTBlocks.CASING_…)` + `.model(steamMultiblockModel(overlayDir))` + `.langValue(英文)` + `.tooltipBuilder(...)`（沿用 `GSEMachines` 现有多方块注册风格）。

### 16. 纹理 → ✅ 外壳按档用 GTCEu 锅炉外壳，控制器叠加层加协同标识

- 外观（`appearanceBlock`）按档：青铜 → `GTBlocks.CASING_BRONZE_SOLID`、钢 → `CASING_STEEL_SOLID`、钛 → `CASING_TITANIUM_STABLE`、钨钢 → `CASING_TUNGSTENSTEEL_ROBUST`（与「四档基准」表的外壳列一致）。
- 控制器正面叠加层：复用 GTCEu 大型锅炉叠加层目录（`block/multiblock/large_boiler` 各档），加一层本模组「协同燃烧」标识（新建叠层或改色），以区别于原大型锅炉。
- 各仓（流体输入/输出、物品输入、消音器、进气室）复用既有纹理，无需新建。
- 控制器模型走 `steamMultiblockModel` 的 `addTextureOverride("all", …)` 模式（沿用粉碎机/熔炉做法）。

---

## 七、架构与文件清单（当前实现入口）

| 文件 | 状态 | 说明 |
| --- | --- | --- |
| `machine/multiblock/BoilerRoomMachine.java` | 已实现 | 主类，四档共用，`maxTemperature` 走构造参数 |
| `registry/GSEMachines.java` | 已实现 | 注册 `BOILER_ROOM_BRONZE/STEEL/TITANIUM/TUNGSTENSTEEL` 四个 `MultiblockMachineDefinition`，同时配置模型 |
| `registry/GSERecipeTypes.java`、`machine/multiblock/BoilerRoomFuelSync.java` | 已实现 | `BOILER_ROOM_RECIPES` 与加载时液体燃料同步 |
| `registry/GSEBoilerPatterns.java` | 已实现，进气室规格已对齐 | 7×11×7 去角长方体；顶面中央条带 1–11 个进气室，第二层火室两侧为空气；最小预览保留中心单进气室朝上 |
| `BoilerRoomMachine#createUI` | 已实现，待外观验收 | 保留滚动状态与节流 ±；新增温度计和粉料剩余进度，不引入模式切换 |
| `gametest/GSEBoilerRoomTests.java` | 自动化回归 | 四档条带全部位置、1/11 个进气室、非法仓室拒绝；四档多仓汇总供气与不足不扣；GUI 进度同步与状态提示；消音器朝外；原生与 GTM Things 创造物品输入总线的水/杂酚油/煤粉/空气完整启动链 |
| 控制器纹理与叠加层 | 待外观验收 | P3#16 是设计要求，资源已接入不代表协同标识等细则全部满足 |
| `data/GSELang.java`、`zh_cn.json` | 已接入 | 见 P3#15，长文本与状态显示待验收 |
| `data/GSERecipes.java` | 已实现 | 四档控制器工作台与组装机配方；材料为对应大型锅炉 + 高压混合燃料锅炉 + 对应板 ×7 |

**前轮回归记录（2026-09-12，单进气室规则阶段）**：非暂停调试日志确认旧预览内部错误进气室后修复；当时全部 70 个 GameTest 与完整构建通过。本次条带更新另增 4 项多仓供气测试，此前通过记录不代表新规则已验证。GUI 外观、四档持续生产和下方用户验收仍待游戏内确认。

**条带更新回归（2026-09-12）**：编译、全部 74 个 GameTest、完整构建通过。定向覆盖四档条带 11 个单仓位置、全 11 仓成型、其他标准仓室拒绝，以及分散库存合计不足不扣、刚好满足与余量保留。复跑 datagen 的 238 个资源文件无变化；设计数字 9 项、生成资产 87 个、语言/Jade 检查通过。手动验收项保持未勾选。

**配方启动修复回归（2026-09-12）**：debug 客户端发现结构预览把背面消音器错误朝上，出口被上一层外壳阻挡；GTCEu 因而在配方修改阶段拒绝燃料配方并保持 `IDLE`。预览已改为朝结构外侧。随后从同一 debug 存档确认，GTM Things 创造物品输入总线内已有煤粉、木炭粉和焦炭粉，但旧实现只扫描具体的 GTCEu 原生 `ItemBusPartMachine`，导致兼容总线中的粉料不可见；现改为扫描控制器的通用物品输入配方能力，并通过机器内部路径扣取粉料。完整启动链覆盖原生总线和 GTM Things 创造总线，含水、杂酚油、煤粉、助燃空气、配方推进和初始升温；全仓库 76 个 GameTest 通过。现有客户端需重启加载新代码，旧结构中的消音器需旋转至背面朝外。

**debug 客户端冒烟验收（2026-09-12）**：用户在现有钨钢锅炉房中保留 GTM Things 创造物品输入总线及其中的协同粉料，重启加载修复后确认配方已经开始执行，随后确认未再发现启动问题。本次验收只覆盖“兼容总线识别粉料并启动液体燃料配方”；四档满温产汽、长时间供气收支、断粉/断气恢复、输出堵塞、存档重载和 GUI/纹理仍按下方清单继续验收。

**持续供气验收（2026-09-12）**：用户继续核验持续供气、空气缓存耗尽、缺气暂停、恢复供气与持续产汽，确认全部行为正常。该结果完成供气运行链验收；当时尚未完成的四档满温目标值在下一条记录中验收，断粉、缺水爆炸、输出/泄压、存档重载及 GUI/纹理仍按下方独立项目验收。

**四档满产验收（2026-09-12）**：用户分别核验四档锅炉房满温、满节流产汽量并确认无问题。Normal/Expert 下青铜、钢、钛、钨钢均达到 `6000 / 13500 / 24000 / 48000 mB/t` 目标，Easy 对应产量为 `2×`。

**协同燃烧边界验收（2026-09-12）**：用户确认锅炉房只有液体燃料与协同粉料同时满足时才能运行；纯液体与纯固体均不能启动，未出现自动降级为单燃料模式。

**结构接口验收（2026-09-12）**：用户确认 `230` 个通用外壳候选位可使用允许的标准接口；顶面中央 `11` 格条带仅接受对应档外壳或 1–11 个蒸汽进气室，正中心不要求必装，其他仓室不能进入条带；普通外壳不设最低数，标准仓室不设合计上限。

**进气规则验收（2026-09-12）**：用户确认未接蒸汽进气室时锅炉房不产汽并显示玩家提示，恢复进气后可继续运行；四档助燃空气消耗量按青铜、钢、钛、钨钢依次为 `50 / 100 / 200 / 400 mB/t`，符合逐档递增规则。

**缺粉冷却验收（2026-09-12）**：用户确认运行中切断协同粉料后燃烧暂停并加速冷却，恢复粉料后继续运行；青铜、钢、钛、钨钢每降低 `1°C` 分别需要 `15 / 10 / 6 / 4 tick`，符合 P1#6 中快于各档停机冷却与升温的硬约束。

---

## 八、孤儿资产消化主线（按 P2 决策更新）

按 P2 决策，锅炉房对孤儿的消化范围已**收窄为仅蒸汽进气室**：

- ✅ **蒸汽进气室** —— 作为协同燃烧硬性前置（P2#10-A），是唯一被锅炉房接管的孤儿资产，且语义最自洽。
- ❌ **工业蒸汽机械方块** —— 外壳大部分与 GTCEu 大型锅炉一致，不必然消费此方块（呼应 `next-machine-candidates.md` 的 S2 需重新评估）。
- ❌ **蒸汽流体输入仓** —— 用户明确**不可用于**锅炉房流体输入（P2#9）。
- ❌ **蒸汽排气仓** —— 用户明确**不用于**锅炉房，蒸汽导出走输出仓、泄压走消音器（P2#11）。

> 当前归属：工业蒸汽机械方块、蒸汽流体输入仓和蒸汽排气仓均已被其他已实现机器使用；锅炉房不使用这些部件不构成新的孤儿资产待办，见[候选清单的资产现状](next-machine-candidates.md#孤儿资产现状)。

---

## 九、验收清单

- [x] debug 客户端中，钨钢锅炉房可从 GTM Things 创造物品输入总线识别协同粉料并启动液体燃料配方。
- [x] 持续供气与空气缓存收支正常；缓存耗尽时暂停燃烧，恢复供气后继续运行并持续产汽。
- [ ] 四档 `BoilerRoomMachine` 均能搭建并识别 7×11×7 去角长方体结构。
- [x] 每档满温满节流产汽 ≥ 目标值（青铜 6000 / 钢 13500 / 钛 24000 / 钨钢 48000 mB/t，Normal/Expert；Easy ×2）。
- [x] 仅协同燃烧模式可运行；无纯液体、无纯固体入口。
- [x] `230` 个通用外壳候选位可替换为允许的标准接口；顶面中央 `11` 格仅外壳/进气室，进气室 1–11 个、正中心非必装；不设普通外壳最低数或标准仓室合计上限。
- [x] 未接蒸汽进气室时不产汽，并有玩家提示；空气消耗量逐档递增（符合 P2#10 序列）。
- [x] 协同缺粉时暂停燃烧并加速冷却（数值符合 P1#6 硬约束）。
- [ ] 运行期水不足触发爆炸（强度 2）；无双爆。
- [ ] 不使用任何蒸汽输入输出仓室（蒸汽流体输入仓/输出仓/排气仓均禁用，物品输入禁用蒸汽物品输入总线）。
- [ ] 泄压走消音器，行为正确。
- [ ] GUI：节流 ± 保留、模式按钮移除、粉料进度条与状态提示正常。
- [ ] 四档控制器可经合成配方获取（升级路径成立；工作台与组装机两版材料一致，均为对应档大型锅炉 + 高压混合燃料锅炉 + 对应板 ×7）。
- [ ] 中英文语言条目齐全；纹理与叠加层正确加载。
- [ ] 难度切换（Easy/Normal/Expert）产出倍率符合 P1#4 / P2#12。

---

## 十、待用户后续决定

**（无）——设计决策已全部收口，四档代码已实现。** 下一步为核对实现差异、完成第九节游戏内验收；实现存在不一致时保留定案要求，单独跟踪修复。
